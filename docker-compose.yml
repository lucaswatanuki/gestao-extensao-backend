version: '2'

services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: sag
      POSTGRES_PASSWORD: develop
      POSTGRES_DB: sag-extensao
    ports: 
      - 5432:5432
  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080